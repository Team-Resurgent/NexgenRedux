name: CMake

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:

  BUILD_TYPE: Release

jobs:
  
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
  
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v3

    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}
      
    - name: Install Zip
      if: ${{ matrix.os == 'ubuntu-latest' }}
      uses: montudor/action-zip@v1

    - name: Zip Windows
      if: ${{ matrix.os == 'windows-latest' }}
      uses: vimtor/action-zip@v1.1
      with:
        files: build/NexgenReduxSetup.exe
        dest:  build/NexgenRedux-PC.zip
        recursive: false
        
    - name: Create Mac Installer
      if: ${{ matrix.os == 'macos-latest' }}
      uses: akiojin/generate-mac-installer-github-action@v0.1.0
      with:
        root-directory: 'build/bin/Release'
        bundle-path: 'build/NexgenRedux.app'
        identifier: com.teamresurgent.NexgenRedux
        
    - name: Zip Mac
      if: ${{ matrix.os == 'windows-latest' }}
      uses: vimtor/action-zip@v1.1
      with:
        files: build/NexgenRedux.app
        dest:  build/NexgenRedux-Mac.zip
        recursive: true

    - name: Create draft Release Windows
      if: ${{ matrix.os == 'macos-latest' }}
      uses: softprops/action-gh-release@v1
      with:
        tag_name: latest
        draft: true
        files: 'build/NexgenRedux-PC.zip'
        
    - name: Create draft Release Linux
      if: ${{ matrix.os == 'ubuntu-latest' }}
      uses: softprops/action-gh-release@v1
      with:
        tag_name: latest
        draft: true
        files: 'build/NexgenRedux-Linux.zip'
        
    - name: Create draft Release Mac
      if: ${{ matrix.os == 'macos-latest' }}
      uses: softprops/action-gh-release@v1
      with:
        tag_name: latest
        draft: true
        files: 'build/NexgenRedux-Mac.zip'


      



        
        

